@model ProductDetailsModel
@*Shim. The gallery itself lives in Views/Shared/_ArProductGallery.cshtml, which
    the PictureZoom override renders too — see the comment at the top of it.*@
@await Html.PartialAsync("_ArProductGallery", Model)